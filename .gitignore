# Dependencies
node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Next.js
/.next/
/out/
/build
/coverage
*.tsbuildinfo
next-env.d.ts

# Env files (secrets) — never commit real values
.env
.env*.local

# Vercel
.vercel/

# OS/editor cruft
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# NOTE: .agent-logs/ is intentionally NOT ignored — it ships with the repo
# per the 8x assignment's capture-setup requirement. Do not add it here.

.vercel
.env*
